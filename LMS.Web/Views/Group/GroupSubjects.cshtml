@model IEnumerable<SubjectDto>
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-white">Subjects List</h1>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
       
        <table class="table table-bordered table-hover table-primary">
            <thead>
                <tr>
                    <th><i class="fas fa-list"></i> Name</th>
                    <th><i class="fas fa-list"></i> Code</th>
                    <th><i class="fas fa-list"></i> Credits</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var subject in Model)
                {
                    <tr>
                        <td>@subject.Name</td>
                        <td>@subject.Code</td>
                        <td>@subject.Credits</td>
                        <td>
                            <a class="btn btn-danger btn-sm" asp-action="SubjectDelete" asp-route-id="@subject.SubjectId">
                                <i class="bi bi-trash"></i>
                            </a>
                            <a class="btn btn-primary btn-sm mr-2" asp-action="SubjectEdit" asp-route-id="@subject.SubjectId">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="row pb-3">
            <div class="col-6">
                <a asp-action="GroupIndex" class="btn-primary btn"><i class="bi bi-plus-square"></i> Back To Groups</a>
            </div>
            <div class="col-6 text-end">
                <a asp-action="GroupAddSubject" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Add New Subject</a>
            </div>
        </div>
    </div>
</div>

<style>
    tr:nth-child(even) {
        background-color: #f8f9fa;
    }

    tr:hover {
        opacity: 0.8;
    }
</style>
